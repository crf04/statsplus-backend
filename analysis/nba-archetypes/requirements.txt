# NBA archetype analysis (scripts and notebooks) runtime dependencies.
#
# Analysis-only dependencies live here rather than in the repository root
# requirements.txt; the root manifest stays limited to backend runtime code.
# requirements-dev.txt includes this file so the focused tests and full dev
# gate install the complete analysis toolchain.
nba_api==1.11.4
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
statsmodels==0.15.0
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
