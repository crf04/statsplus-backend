# NBA archetype analysis (scripts and notebooks) runtime dependencies.
#
# The builder module and its focused tests additionally need pandas, numpy,
# scipy, and statsmodels, which the backend keeps in the repository root
# requirements.txt; this file lists the complete analysis toolchain so a fresh
# checkout of the analysis directory can install everything it runs.
nba_api==1.11.4
numpy==1.26.4
pandas==2.2.2
scipy==1.17.1
statsmodels==0.14.6
matplotlib==3.10.1
seaborn==0.13.2